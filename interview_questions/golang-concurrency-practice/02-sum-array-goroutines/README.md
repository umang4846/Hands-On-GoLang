# 02 - Sum of Array Using Goroutines

## Problem
Split an integer array into two halves. Use two goroutines to compute the sum of each half concurrently, then combine and return the final sum.

## Requirements
- Use channels to collect partial results from each goroutine.

## Example Output
```
Total Sum: 21
```