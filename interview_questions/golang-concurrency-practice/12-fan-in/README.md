# 12 - Fan-In Pattern

## Problem
Merge output from two goroutines (one sending integers, another sending strings) into a single channel.

## Requirements
- Use two goroutines to produce sequences.
- Merge both streams into one channel and print.
